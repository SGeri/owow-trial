services:
  db:
    image: postgres:18
    restart: unless-stopped
    environment:
      POSTGRES_USER: owow
      POSTGRES_PASSWORD: owow
      POSTGRES_DB: owow
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
